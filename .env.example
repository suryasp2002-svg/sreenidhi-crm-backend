# Copy this file to .env and set your values

# Public origin of your backend (used in email links/ICS)
# For local dev: http://localhost:5000
# For hosted (e.g., Render): https://your-backend.onrender.com
API_ORIGIN=http://localhost:5000

# ===== SMTP for sending emails =====
# Gmail SMTP (recommended for dev/testing)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-gmail-address@gmail.com
SMTP_PASS=your-app-password
MAIL_FROM="Sreenidhi Fuels <your-gmail-address@gmail.com>"

# Optional: Logo URL used in meeting emails (44x44 recommended)
# EMAIL_LOGO_URL=https://example.com/path/to/logo.png

# ===== Auth/JWT =====
JWT_SECRET=change_me_strong_secret

# ===== Database (choose ONE of the two sections below) =====

# Option A) Single connection string (recommended for Neon/Vercel)
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require
# PGSSLMODE=require

# Option B) Discrete env vars (good for local dev)
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=crm_db
# PGUSER=postgres
# PGPASSWORD=root123
# PGSSLMODE=disable
